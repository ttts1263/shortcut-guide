import styled from "@emotion/styled"
import { useNavigate } from "react-router-dom"

export function KeywordPage() {
  const navigate = useNavigate()

  return <StyledDiv>asdf</StyledDiv>
}

const StyledDiv = styled.div`
  font-size: 100px;
`
